const main = console.log("<html> <body> <h1> JS </h1> <button>JS</button> </body> </html>");
export default main;
const output = main;
if(main == undefined) {
	console.log("Error: the main constant is undefined.");
          new ReferenceError;
} else {
     const output = main;
         console.log("Success!");
}

